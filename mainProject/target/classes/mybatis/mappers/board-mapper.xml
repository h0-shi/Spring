<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="board">
	  <select id="boardList" resultType="boardDTO">
	  	SELECT board_no, board_title, board_write, board_date, board_count, comment FROM boardview LIMIT 0,10
	  </select>
	  
	  <select id="detail" resultType="boardDTO">
	  	SELECT board_no, board_title, board_content, mno, board_date, board_ip, board_del FROM board WHERE board_no = #{no }
	  </select>
  </mapper>